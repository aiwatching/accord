# Accord v2 Service Config
# Minimal service-side configuration.
# Lives at .accord/service.yaml in each service repository.
# The service reads contracts, registry, and inbox from the hub clone.

version: "1.0"

service: {{SERVICE_NAME}}
  # This service's name (must match entry in team config)

team: {{TEAM_NAME}}
  # The team this service belongs to

hub: {{HUB_URL}}
  # Git URL of the accord-hub repository
  # The hub is cloned to .accord/.hub/ on init
