# Accord v2 Team Config
# Per-team orchestrator configuration.
# Lives at teams/{team}/config.yaml in the accord-hub repository.

version: "1.0"

role: orchestrator

team: {{TEAM_NAME}}
  # Team name (must match entry in root accord.yaml)

project:
  name: {{PROJECT_NAME}}
    # Human-readable project name

services:
  # - name: device-manager
  #   maintainer: ai          # ai | human | hybrid | external
  #   owner: "Agent-1"        # optional: who/what handles this service
  #   repo: git@github.com:org/device-manager.git
  #   local: /opt/repos/device-manager
  #
  # - name: auth-service
  #   maintainer: human
  #   owner: "alice@example.com"
  #   repo: git@github.com:org/auth-service.git
  #
  # - name: db-layer
  #   type: module             # module within a parent service
  #   maintainer: ai
  #   directory: src/db
  #   language: typescript
  #
  # - name: partner-api
  #   maintainer: external     # owned by another team
  #   team: platform-team      # which team owns it

# Optional settings (override org-level defaults)
# settings:
#   sync_mode: on-action
#   require_human_approval: false
#   archive_completed: true

# Optional dispatcher config for the TS agent
# dispatcher:
#   workers: 4
#   poll_interval: 30
#   session_max_requests: 20
#   request_timeout: 300
#   max_attempts: 3
#   model: claude-sonnet-4-5-20250929
#   agent: claude-code
